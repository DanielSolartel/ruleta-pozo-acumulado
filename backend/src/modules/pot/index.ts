// Módulo "pot" — GET /api/game-info y GET /api/pot implementados en V0.3
// (ver routes.ts). El endpoint de giro (POST /api/spins), que también
// vive conceptualmente en este dominio, queda para V0.3.1 (ver spec, H).
export { registerPotRoutes } from "./routes";
